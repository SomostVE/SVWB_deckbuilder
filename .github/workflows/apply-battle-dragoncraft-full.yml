name: Apply Dragoncraft Battle Sim rules

on:
  push:
    branches: [agent/dragoncraft-audit]
    paths:
      - ".github/workflows/apply-battle-dragoncraft-full.yml"
      - "scripts/apply-battle-dragoncraft-full.py"
      - "scripts/apply-battle-dragoncraft-fixes.py"
      - "scripts/check-battle-dragoncraft-full.mjs"
  pull_request:
    paths:
      - ".github/workflows/apply-battle-dragoncraft-full.yml"
      - "scripts/apply-battle-dragoncraft-full.py"
      - "scripts/apply-battle-dragoncraft-fixes.py"
      - "scripts/check-battle-dragoncraft-full.mjs"

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout branch
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          ref: ${{ github.head_ref || github.ref_name }}

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: "24"

      - name: Materialize Dragoncraft rules
        run: |
          if grep -q 'battle-dragoncraft-full-rules' js/battle-engine-v5.js; then
            echo 'Dragoncraft rules already materialized.'
          else
            python3 scripts/apply-battle-dragoncraft-full.py
          fi
          python3 scripts/apply-battle-dragoncraft-fixes.py

      - name: Check JavaScript syntax
        run: |
          node --check js/battle-engine-v5.js
          node --check scripts/check-battle-dragoncraft-full.mjs

      - name: Run Dragoncraft regression
        run: |
          node scripts/check-battle-dragoncraft-full.mjs
          node scripts/check-battle-coverage-100.mjs
          node scripts/check-battle-rules.mjs
          node scripts/check-battle-reactive-events.mjs
          node scripts/check-battle-runecraft-full.mjs
          node scripts/check-battle-swordcraft-full.mjs
          node scripts/check-battle-forestcraft-full.mjs

      - name: Commit tested engine changes
        run: |
          if git diff --quiet -- js/battle-engine-v5.js; then
            echo "No engine changes to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add js/battle-engine-v5.js
          git commit -m "feat: complete Dragoncraft Battle Sim class pass"
          git pull --rebase origin ${{ github.head_ref || github.ref_name }}
          git push origin HEAD:${{ github.head_ref || github.ref_name }}
