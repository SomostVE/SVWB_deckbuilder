name: Apply final Havencraft Battle Sim rules

on:
  push:
    branches: [agent/final-battle-audit]
    paths:
      - ".github/workflows/apply-battle-havencraft-final.yml"
      - "scripts/apply-battle-havencraft-full.py"
      - "scripts/check-battle-havencraft-full.mjs"
  pull_request:
    paths:
      - ".github/workflows/apply-battle-havencraft-final.yml"
      - "scripts/apply-battle-havencraft-full.py"
      - "scripts/check-battle-havencraft-full.mjs"

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout branch
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          ref: ${{ github.head_ref || github.ref_name }}
      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: "24"
      - name: Materialize Havencraft rules
        run: |
          if grep -q 'battle-havencraft-final-full-rules' js/battle-engine-v5.js; then
            echo 'Havencraft rules already materialized.'
          else
            python3 scripts/apply-battle-havencraft-full.py
          fi
      - name: Check syntax
        run: |
          node --check js/battle-engine-v5.js
          node --check scripts/check-battle-havencraft-full.mjs
      - name: Run full class regressions
        run: |
          node scripts/check-battle-havencraft-full.mjs
          node scripts/check-battle-coverage-100.mjs
          node scripts/check-battle-rules.mjs
          node scripts/check-battle-reactive-events.mjs
          node scripts/check-battle-runecraft-full.mjs
          node scripts/check-battle-swordcraft-full.mjs
          node scripts/check-battle-forestcraft-full.mjs
          node scripts/check-battle-dragoncraft-full.mjs
          node scripts/check-battle-abysscraft-full.mjs
          node scripts/check-battle-portalcraft-full.mjs
          node scripts/check-battle-neutral-full.mjs
      - name: Commit tested engine changes
        run: |
          if git diff --quiet -- js/battle-engine-v5.js; then
            echo "No engine changes to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add js/battle-engine-v5.js
          git commit -m "feat: complete full Havencraft Battle Sim class pass"
          git pull --rebase origin ${{ github.head_ref || github.ref_name }}
          git push origin HEAD:${{ github.head_ref || github.ref_name }}
