name: Apply Forestcraft Battle Sim rules

on:
  push:
    branches: [agent/forestcraft-audit]
    paths:
      - ".github/workflows/apply-battle-forestcraft-full.yml"
      - "scripts/apply-battle-forestcraft-full.py"
      - "scripts/apply-battle-forestcraft-fixes.py"
      - "scripts/check-battle-forestcraft-full.mjs"
  pull_request:
    paths:
      - ".github/workflows/apply-battle-forestcraft-full.yml"
      - "scripts/apply-battle-forestcraft-full.py"
      - "scripts/apply-battle-forestcraft-fixes.py"
      - "scripts/check-battle-forestcraft-full.mjs"

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout branch
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          ref: ${{ github.head_ref || github.ref_name }}

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: "24"

      - name: Materialize Forestcraft rules
        run: |
          if grep -q 'battle-forestcraft-full-rules' js/battle-engine-v5.js; then
            echo 'Forestcraft rules already materialized.'
          else
            python3 scripts/apply-battle-forestcraft-full.py
          fi
          if grep -q 'isPixie && name === "\*\*\* the fairy blade"' js/battle-rules.js; then
            echo 'Forestcraft follow-up fixes already materialized.'
          else
            python3 scripts/apply-battle-forestcraft-fixes.py
          fi

      - name: Check changed JavaScript syntax
        run: |
          node --check js/battle-rules.js
          node --check js/battle-engine-v5.js
          node --check scripts/check-battle-forestcraft-full.mjs

      - name: Run Forestcraft regression
        run: |
          node scripts/check-battle-forestcraft-full.mjs
          node scripts/check-battle-coverage-100.mjs
          node scripts/check-battle-rules.mjs
          node scripts/check-battle-reactive-events.mjs
          node scripts/check-battle-runecraft-full.mjs
          node scripts/check-battle-swordcraft-full.mjs

      - name: Commit tested engine changes
        run: |
          if git diff --quiet -- js/battle-rules.js js/battle-engine-v5.js; then
            echo "No engine changes to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add js/battle-rules.js js/battle-engine-v5.js
          git commit -m "feat: complete Forestcraft Battle Sim class pass"
          git pull --rebase origin ${{ github.head_ref || github.ref_name }}
          git push origin HEAD:${{ github.head_ref || github.ref_name }}
